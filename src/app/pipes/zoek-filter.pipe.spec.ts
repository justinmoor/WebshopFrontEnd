import { ZoekFilterPipe } from './zoek-filter.pipe';

describe('ZoekFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ZoekFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
